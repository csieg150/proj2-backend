version: 0.2

phases:
  build:
    commands:
      - echo Entering build phase...
      - echo Build started on `date`
      - mvn package
  post_build:
    commands:
      - echo Build successful
artifacts:
  files:
    - target*/*
  discard-paths: yes
  
cache:
  paths:
    - '/root/.m2/**/*'
  